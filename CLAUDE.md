# CLAUDE.md — Project Instructions

## Project Description

Система компьютерного зрения для идентификации и отслеживания игровых фишек на поле настольной игры через сканер (OpenCV/SIFT). Фишки регистрируются по визуальным признакам и распознаются в реальном времени; данные о положении передаются для расчёта игрового хода.

Игра типа "из точки А в точку Б" с правилами вида "пропусти ход", "иди на N клеток назад/вперёд". Правила настраиваемы — система должна оставаться гибкой без закладывания конкретики заранее.

---

## Tech Stack

- Python, OpenCV (SIFT, CLAHE, FLANN, Homography), NumPy
- Данные хода передаются по HTTP (детали не определены)
- Проектор — заглушка (`projector/projector_controller.py`), интеграция позже
- Никаких дополнительных зависимостей без явного согласования (позволяются, если задача решается проще с ней)

---

## Наблюдения и известные проблемы

При решении задач, не связанных напрямую с кодом (качество детекции, настройка камеры, создание поля, регистрация фишек, производительность) — **сначала прочитать `notes/observations.md` и `notes/issues.md`**. Там зафиксированы реальные условия съёмки, поведение системы и известные проблемы из тестирования.

После прочтения наблюдений: **извлечь из них проблемы, проверить наличие в `notes/issues.md`, дописать новые, отметить решённые с кратким описанием решения.**

---

## Coding Philosophy

### Всегда

- Минимум кода для решения задачи. Никаких абстракций, утилит и фич "на потом".
- Перед изменением кода — прочитать его. Не предлагать правки вслепую.
- Чужой код трогать только при прямой необходимости для задачи.
- Комментарии только там, где логика неочевидна.

### Для сложных / неоднозначных / больших задач

- Перед реализацией: явно сформулировать допущения и предложить 2–3 варианта подхода с кратким сравнением.
- Для простых и очевидных задач — сразу минимальное решение без предварительного разбора.

---

## Project Structure

```
camera/          # CameraManager — захват кадров с IP-камеры / вебкамеры
config/          # settings.py — все константы и параметры в одном месте
core/            # GameScanner — главный оркестратор системы
scanner/         # FeatureExtractor, ObjectDetector — SIFT + FLANN + Homography
memory/          # FeatureStorage (pickle I/O), ObjectRegistry (UUID-реестр объектов)
tracker/         # ObjectTracker, TrackedObject — сглаживание позиций
game/            # TurnManager — очередь ходов, связь игроков с фишками
metrics/         # MetricsTracker, Profiler — fps, proc time, breakdown по операциям
projector/       # ProjectorController — заглушка
notes/           # observations.md, issues.md — журнал тестирования и трекер проблем
ассеты/          # Медиафайлы для презентации
```

Точка входа: `run_scanner.py` → `core/game_scanner.py`

---

## Алгоритмы и техники

### CLAHE (Contrast Limited Adaptive Histogram Equalization)

Применяется перед детектором признаков. В отличие от глобального выравнивания гистограммы, CLAHE делит изображение на мелкие блоки (`CLAHE_TILE_GRID`) и нормализует контраст внутри каждого — с ограничением (`CLAHE_CLIP_LIMIT`), чтобы не усиливать шум. Результат: локальные детали на тёмных/засвеченных участках становятся чёткими, SIFT/AKAZE находит в 2–3 раза больше устойчивых точек на объектах с неравномерным освещением. Параметры: `CLAHE_CLIP_LIMIT` (выше → сильнее контраст, больше шума), `CLAHE_TILE_GRID` (меньше → локальнее эффект).

### Bounding box и центральный кроп

**Bbox (bounding box)** — прямоугольник вокруг обнаруженного объекта. Вычисляется проекцией 4 углов ROI-снимка через гомографию M. Фишка круглая → углы bbox всегда содержат фон (поле, стол). Джиттер bbox — норма: каждый кадр SIFT даёт чуть другой набор инлайеров → M чуть другая → углы смещаются.

**"Восьмёрка"** — самопересекающийся bbox: рёбра пересекаются, образуя фигуру 8 или галстук-бабочку. Возникает когда нестабильная гомография переворачивает один или два угла на противоположную сторону. `cv2.polylines` рисует углы в исходном порядке (0→1→2→3→0), и линии пересекаются. Признак ненадёжного матча — RANSAC нашёл геометрически некорректное решение.

**Центральный кроп** — берётся `HSV_CROP_RATIO` (по умолчанию 0.5) от ширины/высоты bbox вокруг центра. Центральные 25% площади содержат почти только пиксели фишки независимо от фона. Используется для HSV-верификации.

**HSV-верификация** — постфильтр после SIFT. Медиана HSV центрального кропа сравнивается с профилем, записанным при регистрации. Ложные детекции (гомография "уехала" на чужую фишку) отклоняются по цвету. Толерантности: `HSV_H/S/V_TOLERANCE` в settings. Требует перерегистрации фишек, если были зарегистрированы до появления HSV.

### Детектор признаков (SIFT / AKAZE)

Переключается через `settings.DETECTOR_TYPE` ('sift' или 'akaze'). SIFT даёт вещественные дескрипторы (128D float32) — работает с FLANN KD-tree. AKAZE даёт бинарные дескрипторы (M-LDB, uint8) — требует FLANN LSH; более быстрый и лицензионно открытый, но менее устойчив на текстурно-бедных объектах. При смене детектора необходимо перерегистрировать все фишки.

### Бюджет SIFT-точек на кадр

`SIFT_FEATURES` — лимит ключевых точек на весь кадр. Один и тот же набор точек используется для поиска всех зарегистрированных объектов последовательно: сначала FLANN ищет совпадения для chip-5, затем для chip-25, и т.д. Точки — общий ресурс, не принадлежат конкретной фишке. С ростом числа объектов на поле каждой фишке достаётся меньше совпадений → детекция деградирует. Эмпирически: надёжная детекция при 500+ совпадениях (регистрация), на кадре реально 10–130 в зависимости от числа объектов. Решение: увеличить `SIFT_FEATURES` (умеренный рост proc time) или использовать фокусированную детекцию.

### Режимы регистрации

`REGISTRATION_MODE` в settings.py определяет способ захвата снимков:
- **tilt** — ROI фиксирован, пользователь наклоняет объект между снимками (~10° каждый)
- **drag** — drag-выделение для каждого снимка; живая рамка на всех снимках, включая первый
- **click** — первый drag задаёт размер roi_w×roi_h, последующие снимки = клик на центр объекта
- **background** — пользователь убирает все объекты → захват фона → для каждого снимка кладёт объект в новое место → автовыделение через absdiff + морфология. Работает без drag/клика; лучший режим для однородного фона

### Фокусированная детекция и очередь ходов

`TurnManager` (`game/turn_manager.py`) хранит список пар (игрок, chip_id) и текущий индекс. Когда очередь активна (`is_focused() == True`), `tracking_loop` передаёт в детектор только фишку текущего игрока. Все точки кадра фокусируются на одном объекте → 200–400 совпадений вместо 10–20 → стабильная детекция даже для слабо текстурированных фишек. Клавиша **N** двигает очередь, **A** снимает фокус (детектировать всех), **T** добавляет игрока.

---

## Setup

**Python:** venv на Python 3.13, зависимости в `requirements.txt` (numpy, opencv-python)

Перед запуском **обязательно** активировать venv:

```bash
# bash / git bash
source .venv/Scripts/activate

# или cmd / powershell
.venv\Scripts\activate
```

Проверить: `which python` должен показывать путь внутри `.venv/`

Установка зависимостей (если new contributor):

```bash
pip install -r requirements.txt
```

**Запуск** (с логированием в `scanner.log`):

```bash
# bash / git bash — прямой путь к python venv, без source activate
PYTHONIOENCODING=utf-8 .venv/Scripts/python -u run_scanner.py 2>&1 | tee scanner.log
```

---

## Conventions

- Операции настройки (Register, List, Delete, Images) — клавиша + кнопка в окне просмотра (`_compute_button_rects`). Операции игрового процесса (T/N/A — очередь ходов) — только клавиатура, кнопки не добавляются.
- Классы с инкапсуляцией, контекстные менеджеры где уместно
- Конфигурация только через `config/settings.py` — не хардкодить константы в модулях
- UUID для идентификации объектов (`uuid.uuid4()`)
- Сериализация через pickle (`.pkl`), с резервными копиями перед изменением
- Комментарии и сообщения об ошибках на русском языке
- Новые модули — по образцу существующих (класс + методы, без глобального состояния)

---

## MVP Readiness Criteria

- [x] Фишка регистрируется через ROI и сохраняется в реестр
- [x] Зарегистрированная фишка стабильно распознаётся на поле в реальном времени
- [x] Фишки различимы по именам/номерам
- [x] Очередь ходов: система знает, чья сейчас очередь, и фокусирует детекцию на нужной фишке
- [x] Поле создано: клетки размечены, позиция фишки привязана к клетке
- [~] Позиция фишки корректно вычисляется и передаётся — вычисляется + stdout/log ✓; HTTP не реализован
- [x] Движение фишки верно считывается и влияет на игру (FSM: WAITING_MOVE → TURN_DONE)
- [x] Система отрабатывает базовые правила игры (SKIP_TURN, MOVE_FORWARD, MOVE_BACK, финиш)
- [~] Система предоставляет 2+ игрокам поле для игры, где будет выявлен 1 победитель — работает, есть мелкие баги

